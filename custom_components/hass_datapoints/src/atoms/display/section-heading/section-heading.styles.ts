import { css } from "lit";

export const styles = css`
  :host {
    display: block;
  }
  .heading {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--secondary-text-color);
  }
`;
